# Privacy

Observatory has no account, backend, cloud sync, telemetry, advertising, or error-reporting service. Maps and preferences are stored in the browser's IndexedDB. Exports are user-initiated local downloads.

When the user deliberately invokes WebMCP, the selected browser agent receives only the bounded map context returned by that tool. No WebMCP tool permanently purges projects or silently clears map regions.
