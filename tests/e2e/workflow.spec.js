import { test,expect } from '@playwright/test'
test('opens the example map and paints with generated assets',async({page})=>{await page.goto('/');await expect(page.getByRole('heading',{name:'Build worlds one tile at a time'})).toBeVisible();await expect(page.getByRole('button',{name:'Install app'})).toBeVisible();await page.getByRole('button',{name:'Open Storyteller Toolbox'}).click();await expect(page.getByRole('menu',{name:'Storyteller Toolbox'})).toBeVisible();await page.getByRole('button',{name:'Close Storyteller Toolbox'}).click();await page.getByRole('button',{name:/Open The Sunken Watch/}).click();await expect(page.getByText('The Sunken Watch',{exact:true}).first()).toBeVisible();await expect(page.getByRole('button',{name:/^Tile 1, 1:/})).toBeVisible();await page.getByRole('button',{name:'Place object'}).click();await page.getByTitle('Treasure chest').click();await page.getByRole('button',{name:/^Tile 2, 2:/}).click();await expect(page.locator('.placed-object[aria-label="Treasure chest"]')).toHaveCount(2);await page.getByTitle('Switch theme').click();await expect(page.locator('.app-shell')).toHaveClass(/theme-charcoal/)})
test('mobile keeps tools and the map available',async({page})=>{await page.setViewportSize({width:390,height:844});await page.goto('/');await page.getByRole('button',{name:/Open The Sunken Watch/}).click();await expect(page.getByRole('button',{name:'Paint terrain'})).toBeVisible();await expect(page.locator('.map-viewport')).toBeVisible();await expect(page.getByTitle('Worn stone')).toBeVisible()})
