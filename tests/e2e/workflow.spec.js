import { test,expect } from '@playwright/test'
test('opens the example and adds a story element in both themes',async({page})=>{await page.goto('/');await expect(page.getByRole('heading',{name:'Story Shack Observatory'})).toBeVisible();await page.getByRole('button',{name:/Open The Glass Cartographer/}).click();await expect(page.getByText('The Glass Cartographer',{exact:true})).toBeVisible();await page.getByRole('button',{name:'Add character'}).click();await expect(page.locator('.inspector input').first()).toHaveValue('Untitled character');await page.getByTitle('Switch theme').click();await expect(page.locator('.app-shell')).toHaveClass(/theme-charcoal/)})
test('mobile offers the grouped story list',async({page})=>{await page.setViewportSize({width:390,height:844});await page.goto('/');await page.getByRole('button',{name:/Open The Glass Cartographer/}).click();await expect(page.locator('.mobile-list')).toBeVisible();await expect(page.getByText(/Spatial arrangement is available/)).toBeVisible()})
